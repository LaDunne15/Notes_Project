<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="record_model"
            type="com.example.notes.screens.record.RecordViewModel" />
    </data>

    <GridLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:columnCount="3"
        android:orientation="horizontal">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Зберегти"
            android:onClick="@{() -> record_model.onStartRecording()}"
            android:layout_gravity="fill"
            android:layout_columnWeight="1"/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Закрити"
            android:layout_gravity="fill"
            android:layout_columnWeight="1"/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Категорія"
            android:onClick="@{ () -> record_model.onClear()}"
            android:layout_gravity="fill"
            android:layout_columnWeight="1"
            android:enabled="@{record_model.visibility}"
            />

        <EditText
            android:layout_width="match_parent"
            android:layout_columnSpan="3"
            android:text="@={record_model.text}"
            android:inputType="textPersonName"
            android:hint="Заголовок" />

        <EditText
            android:layout_height="match_parent"
            android:layout_width="match_parent"
            android:ems="10"
            android:layout_columnSpan="3"
            android:gravity="start|top"
            android:inputType="textMultiLine"
            android:text="@{record_model.recordsString}"
            android:hint="Великий текст"/>

    </GridLayout>
</layout>